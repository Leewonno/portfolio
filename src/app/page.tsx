import { PageContainer } from "@/features";

export default async function Home() {
  return (
    <div className="w-full min-h-screen flex flex-col items-center px-7.5 py-30 sm:p-20 sm:py-40">
      <PageContainer />
    </div>
  );
}
